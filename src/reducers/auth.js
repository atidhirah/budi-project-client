const reducer = (state = [], action) => {
  switch (action.type) {
    case "LOGIN":
      break;
    case "REGISTER":
      break;
    case "LOGOUT":
      break;
    default:
      return state;
  }
};

export default reducer;
